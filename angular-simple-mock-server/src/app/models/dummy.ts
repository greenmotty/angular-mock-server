import {Content} from "./content";

export interface Dummy {
  content: Content[];
  last: boolean;
  totalPages: number;
  totalElements: number;
  size: number;
  number: number;
  first: boolean;
  numberOfElements: number;
}
